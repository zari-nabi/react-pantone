import styled from "styled-components";
import theme from "../../theme";

const defaultColor = theme.colors.lightBackground;

export const SourceListContainer=styled.ul`
padding:1rem;
background-color: ${defaultColor} 
`;
